# Changesets

Run `pnpm changeset` for changes that affect the published editor package.
Choose the semantic version bump and describe the user-visible behavior.

Changes to tests, tooling, documentation, or the private playground do not
require a changeset unless they alter the published package.
