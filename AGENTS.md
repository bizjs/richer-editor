# AGENTS.md

本文件只定义全仓库公共的研发流程、质量要求和验收标准。产品范围、版本计划与架构演进见 `docs` 目录。

## 基本原则

- 开始修改前，先阅读相关规格、实现、测试和包配置。
- 优先完成满足需求的最小闭环，不顺手重构无关代码。
- 不覆盖贡献者已有但未提交的改动；发现冲突时先说明。
- 不虚构脚本、API 或已经完成的能力，一切以仓库实际内容为准。
- 行为、公开 API 或重要工程约定发生变化时，同步更新文档。
- 除非任务明确要求，不引入与当前需求无关的依赖或抽象。

## TDD 工作流

功能开发和缺陷修复默认采用 Red–Green–Refactor：

1. **Red**：先添加或修改测试，准确描述预期行为，并确认测试因目标行为尚未实现而失败。
2. **Green**：编写使测试通过的最小实现。
3. **Refactor**：在测试保护下改善结构、命名和重复代码，不改变外部行为。
4. **Verify**：运行受影响测试，再运行类型检查、完整测试和构建等相关质量检查。

测试先行不等于只写单元测试。根据变更选择最低且合适的测试层级：

- 纯函数、命令、schema 和转换规则使用单元测试。
- React 状态、Props、ref 和组件交互使用组件测试。
- 键盘、剪贴板、拖放、焦点、浏览器差异等真实交互使用浏览器 E2E。
- 包导出、类型、SSR 和消费者安装行为使用产物消费测试。

以下情况可以不机械地先制造失败测试，但必须说明原因并完成相称验证：

- 仅修改文档、注释或非行为性元数据。
- 纯格式化或已由自动化工具保证的机械变更。
- 搭建尚无测试基础设施的最初脚手架；此时应在同一变更中建立最小测试能力。

修复缺陷时，回归测试必须能够在修复前复现问题。不要先修实现，再写一个无法证明回归的测试。

## 实现标准

- 使用 TypeScript 严格类型，不用无解释的 `any`、`@ts-ignore` 或类型断言掩盖边界问题。
- 保持模块职责单一，优先清晰的组合，不创建没有现实用例的通用抽象。
- 公开 API 应有稳定、清晰的命名和完整类型；破坏性变更必须明确指出。
- 错误应在合适的边界处理，不静默吞掉异常，也不向用户暴露敏感数据。
- 浏览器或 DOM 逻辑不得在模块导入阶段无条件执行，确保 SSR import 安全。
- 新依赖需要有明确用途，并评估体积、维护状态、许可证、重复依赖和运行环境兼容性。
- 样式保持组件作用域，不污染宿主全局元素；交互状态不能只依赖颜色表达。
- 所有可交互控件都应具备可访问名称、键盘操作、可见焦点和合理的焦点恢复。

## 测试标准

- 测试验证用户可观察行为和稳定契约，避免过度依赖实现细节或大面积脆弱快照。
- 每个测试应有明确目的；名称说明场景和预期结果。
- 测试必须可重复、相互隔离，不依赖执行顺序、真实时间、随机网络或外部服务。
- 异步测试应等待可观察结果，不使用任意时长的 sleep 掩盖竞态。
- 正常路径之外，还应覆盖相关的空值、边界、失败、取消和恢复场景。
- 数据格式或 schema 变更必须包含序列化与反序列化往返测试。
- 用户输入、HTML、链接、文件或远程资源相关变更必须覆盖安全和错误路径。
- 不通过降低断言强度、跳过测试或扩大忽略规则来让检查变绿。

## 变更验证

验证范围应与风险相称，至少运行与变更直接相关的测试。具备对应脚本后，通常按以下顺序执行：

1. 受影响的单元或组件测试。
2. TypeScript 类型检查。
3. lint 与格式检查。
4. 完整测试套件。
5. 包构建。
6. 受影响的浏览器 E2E。
7. 涉及发布边界时的 tarball 消费、类型和 SSR import 测试。

只使用根目录或对应 workspace `package.json` 中真实存在的命令。若某项检查尚未配置、无法运行或与变更无关，交付时明确说明，不得声称已经通过。

## 验收标准

一项变更只有满足以下条件才可视为完成：

- 实现符合任务描述和相关规格，没有未说明的范围扩张。
- 新行为有合适层级的自动化测试；缺陷有可证明的回归测试。
- 相关测试、类型检查和构建通过，或已明确记录无法运行的原因。
- 没有新增已知的类型错误、lint 错误、控制台异常或未处理 Promise rejection。
- 公开 API、数据格式、依赖或使用方式的变化已更新相应文档。
- UI 变更已检查键盘操作、焦点、可访问名称、Light/Dark 和必要的响应式状态。
- 涉及包发布时，验证的是实际产物，而不只是源码工作区中的运行结果。
- 交付说明列出改动摘要、实际执行的验证及仍存在的风险或后续事项。

## 代码审查重点

审查时优先关注：

1. 数据丢失、兼容性破坏、安全问题和更新循环。
2. 公开 API、类型、SSR 与包产物问题。
3. 键盘、焦点、无障碍和错误恢复。
4. 缺失或无效的回归测试。
5. 不必要的耦合、依赖、复杂度和包体积增长。

审查结论应指出具体文件和行为影响。不要仅给出风格偏好；非阻塞建议应与正确性问题区分。
